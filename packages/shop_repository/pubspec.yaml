name: shop_repository
description: "repo that handles shops"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^1.10.0
  firebase_auth: ^3.1.0
  cloud_firestore: ^2.5.0
  path: ^1.8.0
  firebase_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  provider: ^6.0.5
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
